# Check Whitespace Action
GitHub Action that checks whitespace usage in the project, and fails if any
non-compliant characters are found. The check includes any trailing whitespace,
and tab indentation used instead of spaces.
